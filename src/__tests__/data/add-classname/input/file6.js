import React from 'react';
import PropTypes from 'prop-types';

const Component = ({ a }) => (
  <div>
    asd
  </div>
);

Component.propTypes = {
  a: PropTypes.number
};

export default Component;
